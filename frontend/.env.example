# Backend API (FastAPI). No trailing slash.
VITE_API_URL=http://localhost:8000

# GitHub Actions: add the same variable as repo secret VITE_API_URL (production API URL).
# Local dev: leave BASE_PATH unset (Vite uses "/"). CI sets BASE_PATH for GitHub Pages.
